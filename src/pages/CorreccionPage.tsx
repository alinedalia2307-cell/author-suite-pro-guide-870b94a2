import ToolPagePlaceholder from "@/components/ToolPagePlaceholder";
import { SpellCheck } from "lucide-react";
export default function CorreccionPage() {
  return <ToolPagePlaceholder icon={SpellCheck} title="Corrección Ortotipográfica" description="Revisa y corrige la gramática, ortografía y estilo de tu manuscrito con ayuda de IA." />;
}
