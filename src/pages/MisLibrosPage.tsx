import ToolPagePlaceholder from "@/components/ToolPagePlaceholder";
import { BookOpen } from "lucide-react";
export default function MisLibrosPage() {
  return <ToolPagePlaceholder icon={BookOpen} title="Mis Libros" description="Gestiona tus manuscritos y proyectos de libro en un solo lugar." />;
}
